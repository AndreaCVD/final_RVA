using System.Drawing;
using UnityEngine;

public class TickTicket : MonoBehaviour
{
    //refrenecia a Images
    public Sprite Tomatoe;
    [SerializeField] PizzaAttach pizzaAttach;

    void RevisarTicks(ingrediente)
    {
        if (ingrediente) //tiene las letras mayusculas o minisculas en su nombre de TomatoeSauce
        {
            //canviar las image (image ref)
            //image tick
        }
        //cuando entra ingrediente en el socket - me pasa el nombre
        //si ese nombre coincide con el nombre de la referencia 
        //image con esa nombre cambio a imagetick


        //}
    }
